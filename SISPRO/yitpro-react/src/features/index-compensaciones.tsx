import ReactDOM from 'react-dom/client';
import Compensaciones from '../modules/Compensaciones/components/Compensaciones';

ReactDOM.createRoot(document.getElementById('compensaciones-root')!)
  .render(
    <Compensaciones />
  );
